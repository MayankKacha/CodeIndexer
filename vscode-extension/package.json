{
  "name": "codeindexer-mcp",
  "displayName": "CodeIndexer — Code Graph & Impact Analysis for AI",
  "description": "Self-hosted code intelligence over MCP. Indexes your repo into a hybrid graph + vector + keyword index so Copilot, Claude, and Cursor can answer 'what calls this?', 'what breaks if I change X?', and 'where do I add Redis?' with grounded citations — not vibes. Auto-indexes on open, reindexes on save, ships with a bundled Python venv (zero manual install).",
  "version": "0.2.2",
  "publisher": "MayankKacha",
  "license": "MIT",
  "icon": "images/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/codeindexer/codeindexer"
  },
  "bugs": {
    "url": "https://github.com/codeindexer/codeindexer/issues"
  },
  "homepage": "https://github.com/codeindexer/codeindexer#readme",
  "keywords": [
    "mcp",
    "model context protocol",
    "code search",
    "semantic search",
    "hybrid search",
    "code graph",
    "call graph",
    "impact analysis",
    "dead code",
    "rag",
    "ai",
    "llm",
    "copilot",
    "claude",
    "cursor",
    "agent",
    "self-hosted",
    "tree-sitter",
    "bm25",
    "vector search"
  ],
  "engines": {
    "vscode": "^1.99.0"
  },
  "categories": [
    "AI",
    "Programming Languages",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "mcpServerDefinitionProviders": [
      {
        "id": "codeindexer-mcp-provider",
        "label": "CodeIndexer MCP"
      }
    ],
    "commands": [
      {
        "command": "codeindexer.startServer",
        "title": "CodeIndexer: Start API Server"
      },
      {
        "command": "codeindexer.stopServer",
        "title": "CodeIndexer: Stop API Server"
      }
    ],
    "configuration": {
      "title": "CodeIndexer MCP",
      "properties": {
        "codeindexer.pythonPath": {
          "type": "string",
          "default": "",
          "description": "Path to Python executable. Leave empty for auto-detection."
        },
        "codeindexer.apiPort": {
          "type": "number",
          "default": 8000,
          "description": "Port for the CodeIndexer API server."
        },
        "codeindexer.autoStartServer": {
          "type": "boolean",
          "default": true,
          "description": "Start the API server automatically when the extension activates. Disable this if you only want the server to start on demand (when an MCP tool is used or a CodeIndexer command is run)."
        }
      }
    }
  },
  "scripts": {
    "compile": "node scripts/bundle-python.js && node esbuild.js",
    "watch": "node scripts/bundle-python.js && node esbuild.js --watch",
    "package": "npx -y @vscode/vsce package --no-dependencies",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.99.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  }
}
